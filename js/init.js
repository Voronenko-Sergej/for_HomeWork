$('.open-order-modal').on('click', function () {
    $('.modal-order').bPopup({
        closeClass: 'close',
    });
});


 $('select').styler();

$('.carusel-ubautUs').slick({
  dots: false,
  infinite: true,
  speed: 500,
  fade: true,
  cssEase: 'linear',
  autoplay: true,
});
		
				